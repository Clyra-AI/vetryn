---
"@vetryn/core": minor
"@vetryn/openrouter": minor
"vetryn": minor
---

Add bounded deterministic evaluation artifacts, model-bound candidate identities, same-invocation OpenRouter execution,
authenticated exact-head receipt storage, durable output staging, and the canonical `vetryn eval` command.
Evaluation preflight keeps credentials and trust state separate, and authenticated pending-head transactions allow
safe retry after interruption between repository and external-head advancement.
