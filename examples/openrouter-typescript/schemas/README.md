# Example-owned schemas

The golden repository will keep its application output schemas here. These schemas represent the
consumer's contract and must remain independent of Vetryn's internal planning and result schemas.
